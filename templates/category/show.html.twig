{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<div class="container pt-5" id="BigBody">
    <div class="bouton">
        <h1>Category</h1>
        {{ include('category/_delete_form.html.twig') }}
    </div>
    

    <table class="table table-striped table--txtlight">
        <tbody>
            <tr>
                <th class="thead--light">Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th class="thead--light">Label</th>
                <td>{{ category.Label }}</td>
            </tr>
        </tbody>
    </table>


    <a class="btn btn-sm btnUserReverse" href="{{ path('category_edit', {'id': category.id}) }}">Modifier</a>

    <a class="btn btn-sm btn-secondary" href="{{ path('category_index') }}">Retour à la liste</a>

    

</div>
{% endblock %}
